[workspace]
resolver = "2"
members = ["kernel", "sprite", "sprig"]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
